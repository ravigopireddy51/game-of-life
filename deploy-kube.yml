apiVersion: apps/v1
kind: Deployment
metadata:
    name: gol-deploy
spec:
    replicas: 1
    template:
      metadata:
       labels:
         app: gol-ctr
      spec:
         containers:
         - name: gol-pod
           image: docker.io/ravigopireddy299/gameoflife-image:latest
           ports:
           - containerPort: 8080
